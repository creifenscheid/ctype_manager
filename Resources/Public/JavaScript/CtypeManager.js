define([
    'jquery'
], function ($) {
    'use strict';

    let CtypeManager = {};

    CtypeManager.switch = function () {
        
    };

    // expose to global
    TYPO3.CtypeManager = CtypeManager;

    return CtypeManager;
});